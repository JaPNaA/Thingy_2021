export class TimeNode {
    /**
     * @param {number} x
     * @param {number} y
     * @param {number} time
     */
    constructor(x, y, time) {
        this.x = x;
        this.y = y;
        this.time = time;
    }
}
